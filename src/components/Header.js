import React from 'react';

const Header = () => (
    <div>
        <h1 id="title">Chemly</h1>
        <h2 className="fade-in">Find chemical listings from around the web</h2>
    </div>
);

export default Header;